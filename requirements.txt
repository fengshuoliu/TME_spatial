# TME Spatial Streamlit app
# Direct runtime Python dependencies inferred from app.py and src/tme_spatial/*.py
# Recommended Python version: 3.11
# These are the packages the code imports directly or uses for TIFF I/O fallbacks.

imageio==2.37.0
ipython==8.30.0
ipywidgets==8.1.5
joblib==1.4.2
matplotlib==3.8.4
numba==0.59.1
numpy==1.26.4
pandas==2.2.2
Pillow==11.1.0
scikit-image==0.25.2
scipy==1.13.1
tifffile==2024.12.12

# Optional acceleration / notebook-style thread setup support
# The app can run without numba, but the original notebook environment referenced it.
# numba>=0.59

# Non-pip / system-level libraries often needed on macOS + Conda for scientific Python
# These are NOT installed by pip requirements.txt; keep them documented here.
# libgfortran5
# libopenblas

# Standard-library modules used by the code and therefore NOT listed above:
# os, pathlib, json, shutil, traceback, uuid, typing, io, re, zipfile, dataclasses
